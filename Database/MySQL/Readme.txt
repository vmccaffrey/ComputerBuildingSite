The file webdev.sql is a single file containing the entire database

The Table folder contains .sql files for each table in the database

They're the same thing, so load either the single file or the whole Table folder